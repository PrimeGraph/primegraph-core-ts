{
  "//": "Deliberately CommonJS: \"type\": \"module\" must never be set here. The generated frontend packages are ESM and the generated backend packages are CommonJS, and a single CJS build is importable from both. A dual CJS+ESM build would let one graph load two copies of the same class, which is exactly the duplicate-nominal-type problem this package exists to remove.",
  "name": "@primegraph/core",
  "version": "1.0.0",
  "description": "Shared cross-package vocabulary for PrimeGraph generated TypeScript packages",
  "license": "MIT",
  "author": "PrimeGraph Generator <generator@primegraph.ai>",
  "repository": {
    "type": "git",
    "url": "https://github.com/primegraph/primegraph-core-ts.git"
  },
  "bugs": {
    "url": "https://github.com/primegraph/primegraph-core-ts/issues"
  },
  "homepage": "https://github.com/primegraph/primegraph-core-ts#readme",
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "setup": "sh scripts/setup.sh",
    "build": "tsc",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "pretest": "npm run build && tsc -p tsconfig.test.json",
    "test": "node --test \"dist-test/**/*.test.js\" \"test/**/*.test.mjs\"",
    "clean": "rm -rf dist dist-test",
    "release": "sh scripts/release.sh"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": ">=22.0.0"
  },
  "keywords": [
    "primegraph",
    "core"
  ],
  "dependencies": {}
}
